# Material resources (WS-03 staged bundle)

`element_presets.v1.json` is a packaged copy of
`schemas/domains/materials/element_presets.v1.json` (WS-08 source of truth).

Re-sync when the schema file changes until CI automation lands.
