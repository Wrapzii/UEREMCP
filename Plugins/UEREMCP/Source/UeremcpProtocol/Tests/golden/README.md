# Golden vectors for UeremcpProtocol

Shared fixed inputs + expected outputs for Python and C++ (WS-14 C-2).

Do not edit `*.expected.*` by hand unless you intentionally change the contract;
prefer:

```bash
python generate_goldens.py
```

Parity rules: `../Docs/CPP_PARITY.md`.
