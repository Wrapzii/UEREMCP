# Runtime view of repo schemas/ for MCP nested-schema publishing (BACKLOG 1.2a).
# Create as a directory junction (not committed):
#   cmd /c mklink /J Plugins\UEREMCP\Content\Schemas schemas
# FUeremcpSchemaPublishing also walks parents for a schemas/ directory.
