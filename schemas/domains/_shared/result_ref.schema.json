{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uneremcp.dev/schemas/domains/_shared/result_ref.schema.json",
  "title": "UEREMCP batch result $ref",
  "description": "Final. Result substitution inside batch operation specifications. Supports both the canonical object form and the REAgentTools dollar-string shorthand. Owner: WS-05. See docs/proposals/ws-05-batch-ref-grammar.md.",
  "oneOf": [
    {
      "title": "objectForm",
      "type": "object",
      "required": ["$ref"],
      "additionalProperties": false,
      "properties": {
        "$ref": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_-]+(\\.[a-zA-Z0-9_]+)+$",
          "description": "operation_id + dotted path into that operation's response JSON, e.g. material.result.primary_asset.",
          "examples": [
            "material.result.primary_asset",
            "niagara.result.created_assets.0.asset_path"
          ]
        }
      }
    },
    {
      "title": "dollarStringForm",
      "type": "string",
      "pattern": "^\\$[a-zA-Z0-9_-]+$",
      "description": "REAgentTools prior art [VERIFIED: batch_workflow_tools.py:37-48]. \"$op_id\" resolves to result.primary_asset, else label, else path.",
      "examples": ["$material", "$niagara"]
    }
  ],
  "examples": [
    { "$ref": "material.result.primary_asset" },
    "$material"
  ]
}
