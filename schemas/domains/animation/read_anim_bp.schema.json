{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uneremcp.dev/schemas/domains/animation/read_anim_bp.schema.json",
  "title": "read_anim_bp specification",
  "description": "One-call read-only AnimBlueprint graph retrieval. The target is request.target.asset_path. Reads nodes, pins and links through the shared UEdGraph reader and adds extensions.animation context. Does not use Blueprint DSL and does not author AnimBP state machines.",
  "type": "object",
  "additionalProperties": false,
  "properties": {},
  "examples": [{}]
}
