{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uneremcp.dev/schemas/domains/materials/create_procedural_texture.schema.json",
  "title": "create_procedural_texture specification",
  "description": "Goal-level procedural Texture2D generation for VFX material slots. CPU pixel fill via FImageUtils::CreateTexture2D [VERIFIED: ImageUtils.h:268] — no Epic MaterialTools equivalent. Also invoked internally from create_vfx_material specification.textures.generate slots. Owned by WS-08.",
  "type": "object",
  "required": ["generate"],
  "additionalProperties": false,
  "properties": {
    "generate": {
      "type": "string",
      "enum": ["noise", "gradient", "voronoi", "ring_mask", "flow_map", "flipbook_atlas", "flipbook_import"],
      "description": "Procedural generator kind. flipbook_atlas packs procedural per-frame cells; flipbook_import is Phase A scaffold for external sheet import (returns partially_completed)."
    },
    "dimensions": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1, "maximum": 4096 },
      "minItems": 2,
      "maxItems": 2,
      "default": [512, 512],
      "description": "[width, height] in pixels."
    },
    "seed": {
      "type": "integer",
      "description": "Deterministic seed. When omitted, defaults to CRC32 of target.asset_path."
    },
    "flipbook": {
      "type": "object",
      "additionalProperties": false,
      "description": "Required when generate is flipbook_atlas. Grid layout for future atlas assembly.",
      "properties": {
        "columns": { "type": "integer", "minimum": 1, "maximum": 64 },
        "rows": { "type": "integer", "minimum": 1, "maximum": 64 },
        "frame_count": { "type": "integer", "minimum": 1, "description": "Active frames; defaults to columns * rows when omitted." }
      },
      "required": ["columns", "rows"]
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "description": "Required when generate is flipbook_import. External sheet path and grid metadata (import not implemented — Phase A scaffold).",
      "properties": {
        "file_path": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute or project-relative path to PNG/TGA flipbook sheet."
        },
        "flipbook": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "columns": { "type": "integer", "minimum": 1, "maximum": 64 },
            "rows": { "type": "integer", "minimum": 1, "maximum": 64 },
            "frame_count": { "type": "integer", "minimum": 1 }
          },
          "required": ["columns", "rows"]
        }
      },
      "required": ["file_path", "flipbook"]
    }
  },
  "examples": [
    {
      "generate": "noise",
      "dimensions": [256, 256]
    },
    {
      "generate": "flow_map",
      "dimensions": [512, 512],
      "seed": 42
    },
    {
      "generate": "ring_mask",
      "dimensions": [512, 512]
    },
    {
      "generate": "flipbook_atlas",
      "dimensions": [512, 512],
      "flipbook": { "columns": 4, "rows": 4, "frame_count": 16 }
    },
    {
      "generate": "flipbook_import",
      "source": {
        "file_path": "Content/__UeremcpTests/Textures/sheet_probe.png",
        "flipbook": { "columns": 4, "rows": 4, "frame_count": 16 }
      }
    }
  ]
}
